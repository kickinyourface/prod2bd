<%= form_for(@tipo_documento) do |f| %>
  <% if @tipo_documento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_documento.errors.count, "error") %> prohibited this tipo_documento from being saved:</h2>

      <ul>
      <% @tipo_documento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod_tipodoc %><br>
    <%= f.number_field :cod_tipodoc %>
  </div>
  <div class="field">
    <%= f.label :nom_tipdoc %><br>
    <%= f.text_field :nom_tipdoc %>
  </div>
  <div class="field">
    <%= f.label :formato %><br>
    <%= f.text_field :formato %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
