<%= form_for(@cotizacion) do |f| %>
  <% if @cotizacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cotizacion.errors.count, "error") %> prohibited this cotizacion from being saved:</h2>

      <ul>
      <% @cotizacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod_cliente %><br>
    <%= f.number_field :cod_cliente %>
  </div>
  <div class="field">
    <%= f.label :canti_productos %><br>
    <%= f.number_field :canti_productos %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
