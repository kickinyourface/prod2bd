<p id="notice"><%= notice %></p>

<h1>Listing Tipo Pagos</h1>

<table>
  <thead>
    <tr>
      <th>Cod pago</th>
      <th>Nom pago</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_pagos.each do |tipo_pago| %>
      <tr>
        <td><%= tipo_pago.cod_pago %></td>
        <td><%= tipo_pago.nom_pago %></td>
        <td><%= link_to 'Show', tipo_pago %></td>
        <td><%= link_to 'Edit', edit_tipo_pago_path(tipo_pago) %></td>
        <td><%= link_to 'Destroy', tipo_pago, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo pago', new_tipo_pago_path %>
